# TestingPageWithRecipes

Requirements needed to run test:

* Java 18 or higher
* Firefox browser installed

Project is using:

* Gradle with Selenium, JUnit and AssertJ

Firefox driver is added to test resources (geckodriver.exe).
